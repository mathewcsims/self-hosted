# Donetick's SQLite database and uploads. Lives on slartibartfast, backed up
# to B2 via Kopia — the DATABASE as a consistent dump, not a file copy of a
# live SQLite file, for the same reason every other database in this repo is
# dumped rather than copied. Never committed.
data/
compose.override.yaml

# Template for vikunja/.env — copy this to .env (gitignored) and fill in a
# real value.
#
#   cp .env.example .env
#   # generate a strong secret with: openssl rand -base64 48 | tr -dc 'A-Za-z0-9' | head -c 64

VIKUNJA_SERVICE_SECRET=CHANGE_ME_LONG_RANDOM_SECRET
